"use strict"; //it will treat all JS code as newer version

// alert( 3 + 3) // this generate output in console but we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")

// sources of JavaScript on internet - tc39.es
//                                   - mdn

let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => range - 2 to power 53
// bigint - for large numbers
// string => "  "
// boolean => true/false
// null => standalone value - representation of empty value which we can assigned to the variable
// undefined =>  variable is defined but the value is not assigned
// symbol => unique

// object

console.log(typeof "om")
console.log(typeof age)
console.log(typeof null) ----- it is a object
console.log(typeof undefined)


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object
